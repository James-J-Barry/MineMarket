# M2: Almanac Lectern

Source: `docs/design.md` → "The Almanac Lectern: upgrades, guides and quests" and "Information as progression".
Goal: the player can **buy upgrades with dollars**, read short guides, and complete the first quests. Everything
per-player, persistent, and testable without a client.

## What to build

### 1. Block: Almanac Lectern
- Shapeless recipe: Lectern + Book + Gold Ingot + Paper. Always craftable (Tier 0, like the Basic Exchange).
- Right-click opens a book-style container screen with **three tabs: Upgrades, Guides, Quests**.
  There is deliberately **no records/net-worth view** here (that's a Tier 5 purchase, Digital Record Keeping).
- Breaking the block loses nothing: progress is stored per player, not in the block.
- Placeholder art is fine (generated textures like the Basic Exchange's). Add to the creative tab.

### 2. Core package `exchange-core/.../progression` (pure Java, unit-tested first)
- `UnlockNode(id, tier, cost cents, parents[], requiredQuest?, grants[])`, `UnlockTree` (loaded from data).
- `PlayerProgress(unlocked set, quest states, stats)`: `canBuy(node)` with reasons, `buy(node)`.
- **Tier gate:** a tier's first purchase requires owning ≥ half of the previous tier's nodes.
- `Quest(id, goal, reward)` evaluated from an **event stream** (`SaleEvent`, `PurchaseEvent`, `NetWorthEvent`,
  `DayRolloverEvent`) so quests are testable without Minecraft.
- Serialization (text, like `DealerStateIO`) for per-player save files.

### 3. Content (data-driven, load from `exchange-core/src/main/resources/realisticmarkets/almanac/`)
Tier 1 nodes (costs from the design doc; tunable):

| Node | Cost | Prereq | Grants (M2) |
|---|---|---|---|
| Bill Clip | $40 | — | unlock flag `bill_clip` (item arrives in M3) |
| Price Board | $75 | — | unlock flag `price_board` |
| Merchant License | $150 | quest 2 "Meet the Spread" | flag `merchant_license` → Basic Exchange spread 20% → 12% for this player (already supported: `licensed` param) |
| Trade Route Crate | $250 | — | flag `trade_route_crate` |

Quests 1–8 (rewards are small dollar payouts or a discount):

| # | Quest | Goal (event-based) | Reward |
|---|---|---|---|
| 1 | First Sale | any sale at a Basic Exchange | $5 |
| 2 | Meet the Spread | buy an item, then sell the same item back | $5 + Spread guide |
| 3 | Flooding the Market | sell ≥256 of one item within one in-game day | $10 + Price Impact guide |
| 4 | Patience Pays | sell an item again after its bid recovered to ≥90% of fair value, having earlier pushed it below 70% | $15 |
| 5 | Diversify | earn ≥$20 from each of three item groups in one in-game day | $25 |
| 6 | Bookkeeper | reach $250 net worth (cash + goods at Dealer bid; lectern announces it) | $10 |
| 7 | Two Markets | *(needs Trade Route Crate, M3)*: define now, leave unreachable | $40 |
| 8 | Save It | hold $500 in cash at once | 10% off Bank Vault (store as a discount flag) |

Guides: 150–250 words each, plain language, one real-world parallel. First five: Money & the Dealer,
The Spread, Price Impact, Recovery & Patience, Diversification. Use the player's own numbers where easy
("You sold 256 wheat for $72.82…"). Guides unlock with their node/quest; readable in the Guides tab and
"tear out" as a vanilla written book item.

### 4. Mod wiring
- `ProgressionService` (like `DealerService`): per-player `PlayerProgress`, saved to
  `<world>/realisticmarkets/players/<uuid>.txt` atomically on change + on stop. Or Fabric data attachments:
  verify with `./scripts/dev.sh api --find Attachment --fabric` before choosing.
- Emit events from `BasicExchangeMenu.sell/buy` (item, qty, cents, group, day).
- Paying for nodes: `Wallet.pay(player, cents)` (bills in inventory). Quest cash rewards: `Wallet.give`.
- Merchant License: pass `licensed = progress.has("merchant_license")` into the Dealer calls in the menu.
- **Decision to make (ask James if unsure): how "grants the recipe" works.** Vanilla can't lock recipes per
  player. Options: (a) gate the crafting result (inspect `ResultSlot`/`CraftingMenu` with `api`) using the
  player's unlock set; (b) the purchase delivers the item directly. Recommendation: (b) for M2/M3 (simple,
  grounded: you buy a license/blueprint), revisit (a) if players need to craft many copies.

### 5. Balance check
Add `sim progression`: a scripted player farming a mix of items and completing quests 1–6; report game-hours
to afford all of Tier 1. Design target: **about 2 hours** of play.

## Done when
- [ ] `progression` unit tests: prerequisites, tier gate, purchase deducts cost, quests 1–6 & 8 complete from
      event streams, save/load round trip.
- [ ] GameTests: buy Bill Clip node with bills (cash deducted, flag set); a sale through `BasicExchangeMenu`
      completes quest 1 and pays $5; Merchant License makes the same 64 wheat pay more than $25.40.
- [ ] Progress survives quit/reload (manual check by James; unit test for the file format).
- [ ] `sim progression` reports Tier 1 reachable in ~2 game-hours (tune costs/rewards if not).
- [ ] `./scripts/dev.sh check` green; design doc updated if numbers changed.
