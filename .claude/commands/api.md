Look up the real Minecraft 26.1 / Fabric API for: $ARGUMENTS

Use `./scripts/dev.sh api --find <text>` (add `--fabric` for Fabric API classes) to locate classes, then
`./scripts/dev.sh api <fully.qualified.Class> [member,member]` for exact signatures. Report the exact class
names, method signatures, and a minimal usage snippet. If you learn something non-obvious, add it to the
"Minecraft 26.1 API notes" table in CLAUDE.md.
